### `usagi init-agent`
AI エージェント用の設定ファイルをプロジェクトに合わせて初期化。

- `.aider.conf.yml`, `.clinerules`, `plandex.yaml` といった AI エージェント用設定ファイルの自動生成。
- プロジェクトの構成（言語、フレームワーク等）に応じた推奨ルールを初期設定。
- 既存のプロジェクトに AI エージェントのコンテキストをすばやく導入。
