# モード定義

usagi.ai には以下の4つのモードが存在します。

## 全体モード (Global Mode)
どのモードにも遷移できる基本の状態です。

## メニューモード (Menu Mode)
画面左側のメニュー（プロジェクト選択など）を操作できるモードです。

## コマンドモード (Command Mode)
画面下部のコマンド入力欄でコマンドを入力できるモードです。

## 実行モード (Execution Mode)
選択したコマンドを実行中のモードです。
